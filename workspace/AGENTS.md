# 智能体（Agents）

## 默认智能体

除非路由绑定将流量指向特定智能体，否则由默认智能体处理所有消息。

## 多智能体部署

在生产环境 OpenClaw 中，可同时运行多个智能体：

- 每个智能体拥有独立的工作区、会话与记忆
- 路由绑定决定由哪个智能体处理每条消息
- 智能体之间相互隔离：无法读取彼此的会话

## 智能体间通信

智能体之间**不**直接对话。协调通过以下方式完成：

1. 共享工作区文件（若已配置）
2. 路由层（绑定可在运行时更新）
3. 人工操作者（通过网关 API 或 CLI）
